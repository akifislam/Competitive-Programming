#include<bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin>>n;

    vector<int> a(n);

    for(auto &x : a)
    {
        cin>>x;
    }

    int res = 0;
    vector<int> cnt(3);

    for (int x = 0; x < 3; x++)
    {
        for (int i = 0; i < n; i++)
        {
            if(a[i]%3 == x)
        }
        
    }
    
    
}